title = "DirectStock gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Ignore local dependency artifacts and CI checkout metadata"
paths = [
  '''\.git/''',
  '''backend/\.venv/''',
  '''frontend/node_modules/''',
]
